# Production Configuration with HTTPS
server.port=8443
app.environment=Produccion

# SSL/TLS Configuration
server.ssl.enabled=true
server.ssl.key-store=/app/certs/keystore.p12
server.ssl.key-store-password=changeit
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=tiendasw

# SSL Protocol Configuration
server.ssl.protocol=TLS
server.ssl.enabled-protocols=TLSv1.2,TLSv1.3

# Database Configuration (usando variables de entorno)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/grocery_delivery?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Isra1234$}
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JWT Configuration
jwt.secret=${JWT_SECRET:enCEL36l/gTeFuUFDZ1rQBzYJQLG16xlcJ+JgicUPiI=}
jwt.expiration=${JWT_EXPIRATION:604800}

# Email Configuration
email.username=${EMAIL_USERNAME:exposicionequipo5@gmail.com}
email.password=${EMAIL_PASSWORD:aszl ykrg udxt bbjf}

# Google Maps API
google.maps.api.key=${GOOGLE_MAPS_API_KEY:YOUR_GOOGLE_MAPS_API_KEY}

# Push Notifications
push.notification.key=${PUSH_NOTIFICATION_KEY:YOUR_PUSH_NOTIFICATION_KEY}

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.utez.edu.mx.tdsw=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN

# Security Headers
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=strict

# Error handling
server.error.whitelabel.enabled=false
server.error.path=/error
